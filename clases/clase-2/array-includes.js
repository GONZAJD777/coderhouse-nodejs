// includes verifica la existencia del elemento y devuelve TRUE or FALSE en funcion de si existe el elemento o no en el array
const nombres = ['Luciana', 'Gala', 'Lau', 'Diego'];

if (nombres.includes('Gala')) {
    console.log('Tenemos incluido este elemento')
} else {
    console.log('Este elemento no está incluido')
}